Box<T>: {
    value: T
    extra: i32
}

Choice<T>:
    Extra(i32)

inner<T> = (value: T) i32 {
    box = Box<T> { value: value, extra: 29 }
    choice = Choice<T>.Extra(box.extra)
    choice ?
        | Extra(found) { found }
}

right<T> = (value: T) i32 {
    inner(value)
}

@export({ right })
