{ io } = std

Person: {
    name: StaticString,
    age: i32,
}

main = () i32 {
    p = Person {
        name: "Alice",
        age: 30
    }
    io.println("${p.name}")
    io.println("${p.age}")
    0
}
