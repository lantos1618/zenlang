{ Json } = traits

Point: {
    x: i32
}

Point.implements(Json<i32>) {
    encode = (value: Point) i32 {
        value.x
    }
}

encode<T: Json<i32>> = (value: T) i32 {
    value.encode()
}

@export({ Point, encode })
