{ io } = std
{ Json, Point } = traits

render<T: Json<StaticString>> = (value: T) StaticString {
    value.encode()
}

main = () i32 {
    point = Point { x: 19 }
    io.println(render(point))
    0
}
