{ io } = std
{ Json } = traits
{ Point } = model

encode<T: Json<StaticString>> = (value: T) StaticString {
    value.encode()
}

main = () i32 {
    point = Point { x: 23 }
    io.println(encode(point))
    io.println(point.pretty())
    0
}
