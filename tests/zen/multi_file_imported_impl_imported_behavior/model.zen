{ PrettyJson } = traits

pub Point: {
    x: i32
}

Point.implements(PrettyJson) {
    encode = (value: Point) str {
        "encoded"
    }

    pretty = (value: Point) str {
        "pretty"
    }
}
