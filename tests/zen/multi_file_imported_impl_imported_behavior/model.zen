{ PrettyJson } = traits

Point: {
    x: i32
}

Point.implements(PrettyJson) {
    encode = (value: Point) StaticString {
        "encoded"
    }

    pretty = (value: Point) StaticString {
        "pretty"
    }
}

@export({ Point })
