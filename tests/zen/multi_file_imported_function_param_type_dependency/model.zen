{ Json } = traits

Point: {
    x: i32
}

Point.implements(Json<i32>) {
    encode = (value: Point) i32 {
        value.x
    }
}

make_point = () Point {
    Point { x: 127 }
}

encode_point = (value: Point) i32 {
    value.encode()
}

@export({ Point, make_point, encode_point })
