{ io } = std

inner<T> = (value: T) T {
    return value
}

outer<T> = (value: T) T {
    a = inner(value)
    b = inner(value)
    return a
}

main = () i32 {
    io.println("${outer(12)}")
    return 0
}
