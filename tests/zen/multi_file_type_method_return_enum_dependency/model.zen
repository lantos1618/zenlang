{ Option } = types

pub Box<T>: {
    value: T
}

pub Box.wrap<T> = (self: Box<T>) Option<T> {
    return Option<T>.Some(self.value)
}

pub Box.value_or<T> = (self: Box<T>, fallback: T) T {
    value = self.wrap<T>()
    value ?
        | Some(inner) { inner }
        | None { fallback }
}
