{ Option } = types

Box<T>: {
    value: T
}

Box.wrap<T> = (self: Box<T>) Option<T> {
    Option<T>.Some(self.value)
}

Box.value_or<T> = (self: Box<T>, fallback: T) T {
    value = self.wrap()
    value ?
        | Some(inner) { inner }
        | None { fallback }
}

@export({ Box })
