{ io } = std
{ PrettyJson } = traits

Point: {
    x: i32
}

Point.implements(PrettyJson) {
    encode = (value: Point) str {
        "encoded"
    }

    pretty = (value: Point) str {
        "pretty"
    }
}

render<T: PrettyJson> = (value: T) str {
    value.encode()
}

main = () i32 {
    point = Point { x: 31 }
    io.println(render(point))
    io.println(point.pretty())
    0
}
