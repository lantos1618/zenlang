Result<T, E>:
    Ok(T),
    Err(E)

Result.unwrap_err<T, E> = (self: Self, fallback: E) E {
    self ?
        | Ok(_) { fallback }
        | Err(error) { error }
}

@export({ Result, Result.unwrap_err })
