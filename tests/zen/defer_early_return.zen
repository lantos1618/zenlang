{ io } = std

cleanup = () void {
    io.println("cleanup early")
}

process = (flag: bool) i32 {
    @this.defer(cleanup())
    flag ?
        | true { 7 }
        | false { 3 }
}

main = () i32 {
    io.println("before")
    result = process(true)
    io.println("result: ${result}")
    0
}
