{ Option } = types

wrap<T> = (value: T) Option<T> {
    Option<T>.Some(value)
}

unwrap<T> = (value: Option<T>, fallback: T) T {
    value ?
        | Some(inner) { inner }
        | None { fallback }
}

@export({ wrap, unwrap })
