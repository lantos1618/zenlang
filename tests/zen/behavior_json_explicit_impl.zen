{ io } = std

Point: {
    x: i32
}

Json: behavior {
    to_json: (Self) StaticString
}

Point.implements(Json) {
    to_json = (value: Point) StaticString {
        "{\"x\":1}"
    }
}

main = () i32 {
    p = Point { x: 1 }
    io.println(p.to_json())
    0
}
