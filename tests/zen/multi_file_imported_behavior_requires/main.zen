{ io } = std
{ Json, Point } = traits

Point.requires(Json<str>)

encode<T: Json<str>> = (value: T) str {
    value.encode()
}

main = () i32 {
    point = Point { x: 41 }
    io.println(encode(point))
    0
}
