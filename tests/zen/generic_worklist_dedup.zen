{ io } = std

inner<T> = (value: T) T {
    value
}

left<T> = (value: T) T {
    inner(value)
}

right<T> = (value: T) T {
    inner(value)
}

main = () i32 {
    a = left(12)
    b = right(30)
    io.println("${a + b}")
    0
}
