{ io } = std

identity<T> = (value: T) T {
    value
}

main = () i32 {
    x = identity(42)
    y = identity(7)
    io.println("${x}")
    io.println("${y}")
    io.println(identity<str>("ok"))
    0
}
