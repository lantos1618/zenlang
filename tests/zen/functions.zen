{ io } = std

square = (x: i32) i32 {
    x * x
}

sum_of_squares = (a: i32, b: i32) i32 {
    square(a) + square(b)
}

main = () i32 {
    io.println("${square(5)}")
    io.println("${sum_of_squares(3, 4)}")
    0
}
