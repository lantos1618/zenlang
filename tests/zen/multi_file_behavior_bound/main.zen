{ io } = std
{ Json } = traits

Point: {
    x: i32
}

Point.implements(Json<Point>) {
    encode = (value: Point) Point {
        value
    }
}

encode<T: Json<T>> = (value: T) T {
    value.encode()
}

main = () i32 {
    point = Point { x: 11 }
    encoded = encode(point)
    io.println("${encoded.x}")
    0
}
