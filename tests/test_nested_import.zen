// Test importing a struct type from stdlib

{ io } = @std
{ GPA } = @std.memory.gpa

main = () i32 {
    io.println("Testing struct import...")

    // Create GPA directly without calling gpa_new
    alloc = GPA { id: 0 }

    io.println("Success!")
    0
}
