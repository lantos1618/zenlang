// build.zen - deterministic project graph

{ Builder, BuildConfig, BuildError, Executable, Test } = @builtin.build

build = (b: Builder) Result<BuildConfig, BuildError> {
    b.add(Executable {
        name: "myapp",
        main: "main.zen",
        out_dir: "build/",
    })

    b.add(Test { root: "test.zen" })

    .Ok(b.config())
}
