// Parse-only placeholder for @std.concurrency.primitives.atomic.
//
// Atomic APIs are stdlib-owned wrappers over guarded compiler hooks. Restore
// them only with parser, typechecker, build, and docs-truth evidence.
