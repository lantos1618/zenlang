// Gated io_uring sketch placeholder.
//
// @std.io.mux.uring is rejected before loading until host-effect declarations,
// allocator ownership, and async runtime semantics are implemented. Keep this
// path visible for the import gate, but do not park syscall-heavy sketch code in
// the live stdlib tree without parser, typechecker, build, and docs-truth
// evidence.
