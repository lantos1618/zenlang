// Experimental std facade placeholder.
//
// Root imports such as `{ io } = std` are currently handled by the compiler
// front end and C backend. This file must not re-export broad stdlib sketches
// until those modules parse, typecheck, build, and have docs-truth evidence.
