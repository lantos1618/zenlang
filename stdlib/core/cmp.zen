// Comparison behaviors. A type implements `Eq` to be usable as an element of
// equality-based collections (`Set`, future `HashMap`).
// `{ Eq } = std.core.cmp`

pub Eq: behavior {
    eq: (Self, Self) bool
}
